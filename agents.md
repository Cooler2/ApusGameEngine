# AGENTS.md

Instructions for AI coding agents working in this repository live in one place:
[`CLAUDE.md`](CLAUDE.md). Read it first - it covers building, code style, string types,
the module layout, tests and refactoring rules, and applies to any agent, not only Claude.

This file is only a pointer, so the two cannot drift apart; do not copy content here.
